import React from 'react';
import './Impact.css';

const Impact = () => {
    return (
        <div className="impact-container">
            <h1>L'impact psychologique du chômage</h1>
            <button>Register</button>
            <button>Login</button>
            <button>Logout</button>
        </div>
    );
};

export default Impact;
